# 当前进度

## 2026-09-07 · 诺斯塔之塔 v1

独立克隆 `https://github.com/yutio8888/sgstory`，分支 `codex/nousta-tower`。

已完成：基于原作者公开双时态地图改编 11 层／22 场景；保留森林篇入口与全部原路线；新增自由切换、手记、已探索地图、按需提示、守卫多解法、一次性遗物与三结局。来源与明确改写部分见 [NOUSTA.md](NOUSTA.md)。

本次执行验证：

- `npm ci --allow-remote=all`：锁定依赖安装成功，npm 报告 0 漏洞。
- Python 独立虚拟环境安装 fonttools/brotli；构建成功嵌入文楷字体。
- `npm test` 成功（Node 26.8.1 / npm 12.0.2）：原规则、冒烟、五条森林路线、新增塔篇规则及实际 HTML 集成全部通过。[完整输出](evidence/nousta-v1-tests-2026-09-07.txt)。
- Codex 内置浏览器在 `http://127.0.0.1:8000/` 实际点击：开场→预设角色→塔基，刷新恢复塔基进度，切换过去→读书→学符文→送餐通过守卫→取遗物→塔顶安抚幼龙→切回现在→和平结局。结局后侧栏回退正确恢复塔顶选择，桌面截图检查无布局遮挡。
- `git diff --check` 通过。

未覆盖：本次未运行 Chromium/Firefox/WebKit 三浏览器矩阵、真实移动设备、Safari 或历史用户存档文件；仓库 CI 的 Node 22 环境本次未本地复跑。不将 jsdom 等同于浏览器矩阵。未推送远程，也未发布 GitHub Pages。

试玩：构建结果 `dist/index.html` 可单文件打开；`npm run serve` 启动 8000 端口，开场选“探索诺斯塔之塔”。本次已经启动该端口用于浏览器验证。dist 按仓库原约定被 git 忽略，不是封存发行包。

后续验收：若发布，应先在 CI 的 Node 22 下通过测试，并补手机窄屏与目标浏览器的完整路线/持久化验收；再决定是否推送触发 Pages。下一次仍从此文档接手。
